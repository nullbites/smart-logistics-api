import type { GraphEdge, VehicleProfile } from '../domain/types';

export function canTraverse(_edge: GraphEdge, _vehicle: VehicleProfile): boolean {
  void _edge;
  void _vehicle;
  throw new Error('not implemented');
}
