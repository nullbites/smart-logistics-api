import type { Graph, GraphEdge, GraphNode } from './types';

export function buildGraph(
  _nodes: GraphNode[],
  _edges: GraphEdge[],
  _heuristicScale?: number | null,
): Graph {
  throw new Error('not implemented');
}
